[build]
builder = "NIXPACKS"
watchPatterns = ["policyai-backend/**"]

[deploy]
startCommand = "cd policyai-backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
